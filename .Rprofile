# options(help_type = "html", width = 100, device = "quartz", 
#         defaultPackages=c(getOption("defaultPackages"), "colorout"))

options(help_type = "html", width = 100, device = "quartz") 

# if (is.element("colorout", installed.packages()[,1]))
#     library("colorout")
